VITE_API_BASE_URL=https://campus-tracker-backend.onrender.com/api
# GPS now lives on the backend (GPS_API_BASE_URL / GPS_API_USERNAME):
# the provider sends no CORS headers and its credential must not ship
# in the browser bundle.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_browser_key
